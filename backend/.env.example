# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/felicity

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_change_this_in_production
JWT_EXPIRY=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Admin Credentials (for seed script)
ADMIN_EMAIL=admin@felicity.com
ADMIN_PASSWORD=Admin@1234

# ─── Email Configuration (required for ticket emails) ─────────────────────────
#
# HOW TO GET A GMAIL APP PASSWORD:
#   1. Go to myaccount.google.com → Security
#   2. Enable 2-Step Verification (required)
#   3. Go to Security → App Passwords
#   4. App name: "Felicity" → click Create
#   5. Copy the 16-char password Google gives you (no spaces)
#   6. Paste it as EMAIL_PASSWORD below
#
# NOTE: Use the App Password, NOT your real Gmail password.
# If you don't want to use Gmail, use Mailtrap (mailtrap.io) for dev testing.

EMAIL_SERVICE=gmail
EMAIL_USER=your_gmail_address@gmail.com
EMAIL_PASSWORD=abcdefghijklmnop

# ─── Alternative: Mailtrap (catches emails without sending — great for testing) ─
# Leave EMAIL_SERVICE blank, and set these instead:
# EMAIL_SERVICE=
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=587
# EMAIL_USER=<mailtrap-username>
# EMAIL_PASSWORD=<mailtrap-password>
