{
  "name": "felicity-backend",
  "version": "1.0.0",
  "description": "Backend for Felicity Event Management System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed:admin": "node seedAdmin.js",
    "test": "NODE_ENV=test jest --runInBand",
    "test:watch": "NODE_ENV=test jest --watch --runInBand",
    "test:coverage": "NODE_ENV=test jest --coverage --runInBand",
    "test:auth": "NODE_ENV=test jest __tests__/auth.test.js --runInBand",
    "test:participants": "NODE_ENV=test jest __tests__/participants.test.js --runInBand",
    "test:events": "NODE_ENV=test jest __tests__/events.test.js --runInBand",
    "test:registrations": "NODE_ENV=test jest __tests__/registrations.test.js --runInBand",
    "test:admin": "NODE_ENV=test jest __tests__/admin.test.js --runInBand",
    "test:edge": "NODE_ENV=test jest __tests__/edgecases.test.js --runInBand"
  },
  "keywords": [
    "felicity",
    "event-management",
    "mern"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.5",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "multer": "^2.0.2",
    "nodemailer": "^6.9.7",
    "qrcode": "^1.5.3",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.4"
  }
}
